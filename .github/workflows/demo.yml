name: Demo Workflow

on:
  workflow_dispatch:

jobs: 
  print:
    runs-on: ubuntu-latest
    steps:
      - name: print to console
        run: echo "Hello GH Actions!"
      - name: print a few lines
        run: 
          echo "first line!"
          echo "second line!"
